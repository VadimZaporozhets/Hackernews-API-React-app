const DEFAULT_QUERY = 'js';
const DEFAULT_HPP = '10';

const largeColumn = {
    width: '40%'
};
const midColumn = {
    width: '30%'
};
const smallColumn = {
    width: '10%'
};

export {DEFAULT_HPP, largeColumn, midColumn, smallColumn, DEFAULT_QUERY};